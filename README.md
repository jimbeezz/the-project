# Code Quality Assessment Tool

[![CI/CD Status](https://github.com/USERNAME/the-project/workflows/Code%20Quality%20Assessment%20CI%2FCD/badge.svg)](https://github.com/USERNAME/the-project/actions)

## Описание

**Code Quality Assessment Tool** — это инструмент для автоматического оценивания качества студенческого Python кода. Проект решает реальную проблему в области ИИ в образовании, позволяя преподавателям быстро и объективно оценивать качество кода студентов по набору метрик.

### Проблема

При проверке студенческих заданий преподаватели тратят много времени на ручную оценку качества кода. Необходим автоматизированный инструмент, который:
- Оценивает соблюдение стандартов кодирования (PEP 8)
- Анализирует сложность кода
- Проверяет наличие документации
- Обнаруживает дублирование кода
- Генерирует подробные отчеты с рекомендациями

### Решение

Инструмент автоматически анализирует Python код и выставляет оценку на основе следующих метрик:
- **PEP 8 Compliance** (30% веса) — проверка стиля кода
- **Cyclomatic Complexity** (25% веса) — анализ сложности функций
- **Docstring Coverage** (25% веса) — наличие документации
- **Code Duplication** (20% веса) — обнаружение дублирования

## Установка

### Требования

- Python 3.8+
- pip

### Установка зависимостей

```bash
# Клонировать репозиторий
git clone https://github.com/USERNAME/the-project.git
cd the-project

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# На Windows:
venv\Scripts\activate
# На Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

## Использование

### Базовое использование

Анализ одного файла:
```bash
python -m src.main path/to/file.py
```

Анализ директории:
```bash
python -m src.main path/to/directory/
```

### Форматы отчетов

**Текстовый отчет (по умолчанию):**
```bash
python -m src.main data/students/ --format text --output report.txt
```

**HTML отчет:**
```bash
python -m src.main data/students/ --format html --output report.html
```

**JSON отчет:**
```bash
python -m src.main data/students/ --format json --output report.json
```

### Примеры

#### Пример 1: Анализ студенческого кода

```bash
python -m src.main data/students/ --format html --output reports/student_report.html
```

#### Пример 2: Анализ собственного кода проекта

```bash
python -m src.main src/ --format html --output reports/project_report.html
```

## Примеры вывода

### Текстовый отчет

```
================================================================================
CODE QUALITY ASSESSMENT REPORT
================================================================================
Generated: 2024-01-15 10:30:00

Files analyzed: 3
Average overall score: 72.50/100

--------------------------------------------------------------------------------
File: data/students/sample_student_code.py
--------------------------------------------------------------------------------

Overall Score: 68.50/100

PEP 8 Compliance: 75.00/100
  Violations: 8
  Issues:
    - Line 45: Line 45 exceeds 79 characters (95 chars)
    - Line 10: Line 10 has trailing whitespace

Cyclomatic Complexity:
  Average: 6.50
  Maximum: 12
  Functions:
    - complex_function (line 8): 12
    - calculate_sum (line 4): 1

Docstring Coverage: 25.00%
  Functions: 1/4
  Classes: 0/1

Code Duplication: 15.00%
  Duplicate blocks: 3

Code Statistics:
  Lines of code: 50
  Functions: 4
  Classes: 1

================================================================================
RECOMMENDATIONS
================================================================================

data/students/sample_student_code.py:
  - Improve PEP 8 compliance by fixing style violations
  - Reduce cyclomatic complexity by breaking down complex functions
  - Add docstrings to functions and classes for better documentation
  - Reduce code duplication by extracting common code into functions
```

### HTML отчет

HTML отчет содержит интерактивную визуализацию с цветовой индикацией:
- 🟢 Зеленый: Высокий балл (≥70)
- 🟡 Желтый: Средний балл (50-69)
- 🔴 Красный: Низкий балл (<50)

## Структура проекта

```
the-project/
├── src/                    # Исходный код
│   ├── __init__.py
│   ├── code_analyzer.py    # Основной модуль анализа
│   ├── report_generator.py  # Генератор отчетов
│   └── main.py             # Точка входа
├── tests/                  # Тесты
│   ├── __init__.py
│   ├── test_code_analyzer.py
│   └── test_report_generator.py
├── data/                   # Данные
│   └── students/           # Код студентов для анализа
│       └── sample_student_code.py
├── reports/                # Сгенерированные отчеты
├── docs/                   # Документация
├── .github/
│   └── workflows/
│       └── ci.yml          # CI/CD конфигурация
├── requirements.txt        # Зависимости
├── .gitignore
└── README.md
```

## CI/CD

Проект использует GitHub Actions для автоматизации:

### Возможности CI/CD

1. **Автоматическая проверка кода**
   - Запуск тестов при каждом push и pull request
   - Проверка PEP 8 с помощью flake8
   - Форматирование с помощью black

2. **Анализ студенческого кода**
   - Автоматический анализ кода из папки `data/students/`
   - Генерация HTML и JSON отчетов
   - Сохранение отчетов как artifacts

3. **Покрытие тестами**
   - Генерация отчетов о покрытии кода
   - Загрузка в Codecov

4. **Расписание (Scheduled Workflow)**
   - Ежедневный запуск в 2:00 UTC
   - Автоматический анализ и обновление отчетов

5. **Ручной запуск (Workflow Dispatch)**
   - Возможность запустить workflow вручную через GitHub UI

### Статус CI/CD

Проверьте статус последнего запуска: [![CI/CD Status](https://github.com/USERNAME/the-project/workflows/Code%20Quality%20Assessment%20CI%2FCD/badge.svg)](https://github.com/USERNAME/the-project/actions)

## Тестирование

Запуск тестов:
```bash
pytest tests/ -v
```

С покрытием кода:
```bash
pytest tests/ -v --cov=src --cov-report=html
```

Отчет о покрытии будет сохранен в `htmlcov/index.html`.

## Метрики качества

### PEP 8 Compliance
Проверяет:
- Длина строк (максимум 79 символов)
- Отступы (4 пробела)
- Трейлинг пробелы
- Базовые стилистические правила

### Cyclomatic Complexity
Измеряет сложность функций на основе:
- Количества условных операторов (if, while, for)
- Логических операций
- Обработчиков исключений

### Docstring Coverage
Проверяет наличие документации:
- В функциях
- В классах
- Общий процент покрытия

### Code Duplication
Обнаруживает повторяющиеся блоки кода:
- Последовательности из 3+ строк
- Процент дублирования в файле

## Оценка

Итоговая оценка (0-100) рассчитывается по формуле:

```
Overall Score = 
    PEP 8 Score × 0.30 +
    Complexity Score × 0.25 +
    Docstring Score × 0.25 +
    Duplication Score × 0.20
```

Где:
- **Complexity Score** = max(0, 100 - (avg_complexity - 1) × 10)
- **Duplication Score** = max(0, 100 - duplication_percentage × 2)

## Вклад в проект

1. Fork репозитория
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Проект разработан для курса по ИИ в образовании.

## Ссылки

- [PEP 8 Style Guide](https://pep8.org/)
- [Cyclomatic Complexity](https://en.wikipedia.org/wiki/Cyclomatic_complexity)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
